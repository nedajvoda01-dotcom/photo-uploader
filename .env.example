# Authentication
AUTH_SECRET=your-long-random-secret-key-here
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Default region for users (used for legacy/file-based auth)
DEFAULT_REGION=MSK

# Yandex Disk Integration
YANDEX_DISK_TOKEN=your-yandex-disk-oauth-token
# Get from: https://yandex.ru/dev/disk/poligon/

# Legacy Upload Configuration (optional, for backward compatibility)
UPLOAD_DIR=/mvp_uploads
UPLOAD_MAX_MB=20

# Admin User (for environments without database)
# Option 1: Plain password (quick setup, less secure)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=your-secure-password

# Option 2: Bcrypt hash (more secure, recommended for production)
# ADMIN_PASSWORD_HASH=$2b$10$XAFke6qJqObeuIa.1kC3T.ufP4078lWsDvwLIfMCWBhdT2gAFD3Gi
# Generate with: node -e "const bcrypt = require('bcryptjs'); bcrypt.hash('your-password', 10, (err, hash) => { console.log(hash); });"
# Note: In .env.local files, escape $ signs with backslashes: \$2b\$10\$...
# Note: In Vercel UI, use the hash as-is without escaping

# Debug Mode (optional)
# AUTH_DEBUG=1

# Vercel Postgres (required for database mode)
# These are automatically provided by Vercel when you add Postgres storage
# POSTGRES_URL="postgres://..."
# POSTGRES_PRISMA_URL="postgres://..."
# POSTGRES_URL_NO_SSL="postgres://..."
# POSTGRES_URL_NON_POOLING="postgres://..."
# POSTGRES_USER="..."
# POSTGRES_HOST="..."
# POSTGRES_PASSWORD="..."
# POSTGRES_DATABASE="..."
